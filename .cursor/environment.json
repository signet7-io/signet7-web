{
  "name": "signet7-web",
  "install": "python3 -m compileall -q tests",
  "terminals": [
    {
      "name": "site",
      "command": "python3 tests/serve_site.py --port 8080 --bind 0.0.0.0",
      "description": "Serves the static signet7.io export at http://127.0.0.1:8080, resolving extensionless links the way GitHub Pages does. Open pages as /index.html or /product, /docs.html or /docs, and so on."
    }
  ],
  "ports": [
    {
      "name": "site",
      "port": 8080
    }
  ]
}
